---
id: {{FEATURE_ID}}
title: {{FEATURE_TITLE}}
type: feature-test-spec
status: draft
owner: {{OWNER}}
change_types: "{{CHANGE_TYPES}}"
last_updated: {{DATE}}
template_version: {{TEMPLATE_VERSION}}
template_profile: {{TEMPLATE_PROFILE}}
template_language: {{TEMPLATE_LANGUAGE}}
---

# 测试方案

## 测试目标

说明这次变更要重点验证的风险点：功能正确性、接口兼容性、事务一致性、缓存一致性、权限安全、性能波动等。

## 测试范围矩阵

| 验证项 | 是否需要 | 说明 |
|--------|----------|------|
| 单元测试 | 是/否 | 需要覆盖的规则或分支 |
| 集成测试 | 是/否 | 需要验证的组件协作 |
| 接口测试 | 是/否 | 需要验证的 HTTP/RPC 接口 |
| 消息/任务测试 | 是/否 | 需要验证的异步流程 |
| 人工验收 | 是/否 | 需要人工确认的界面或流程 |

## 必做检查

- 单元测试
- 集成测试
- 如有需要，补充人工或探索式验证

## 测试数据与环境准备

- 需要哪些测试账号、字典、库表数据、消息、缓存
- 是否需要测试桩、Mock 服务、联调环境、灰度环境
- 是否需要准备历史数据、失败数据、重复数据、并发数据

## 关键测试用例

- 正常路径
- 边界值与非法参数
- 权限、幂等、重复提交、并发更新
- 外部依赖失败、超时、重试、降级
- 回滚与兼容性场景

## 回归命令与证据

- 建议记录要执行的测试类、测试套件、`mvn/gradle` 命令
- 建议记录要检查的 SQL、日志、指标、Topic、重试记录
- 说明 PR 或发布记录中需要附哪些日志、截图、执行结果

## 发布前后验证

- 上线前必须回归哪些关联功能
- 灰度期间观察哪些指标和日志
