---
id: project-security
title: 项目安全规范
type: project-security
status: active
owner: {{OWNER}}
last_updated: {{DATE}}
template_version: {{TEMPLATE_VERSION}}
template_profile: {{TEMPLATE_PROFILE}}
template_language: {{TEMPLATE_LANGUAGE}}
---

# 项目安全规范

## 安全目标与适用范围

说明项目最重要的安全目标，例如防越权、防泄露、防篡改、防误操作、防供应链风险。

## 资产与敏感数据分类

- 识别账号、令牌、密钥、手机号、身份证号、交易数据等敏感资产
- 明确哪些字段禁止落日志、禁止明文传输、必须脱敏展示

## 认证、授权与审计

- 使用何种认证机制：Session、JWT、OAuth2、网关鉴权、内部签名
- 角色、租户、数据权限、操作权限分别在哪一层校验
- 哪些关键操作必须产生日志审计、审批记录或追踪信息
- 对内部回调、消息消费、任务触发是否需要额外校验来源和业务身份

## 输入校验与反序列化安全

- 所有外部输入必须经过参数校验和边界检查
- 严格限制反序列化、动态脚本、文件上传、富文本、表达式执行等高风险点
- 禁止信任来自前端或上游的权限、金额、状态等关键字段

## 敏感数据与日志脱敏

- 明确哪些字段允许落业务日志，哪些只能落审计日志，哪些完全禁止落日志
- 明确手机号、证件号、token、签名串、报文体、SQL 参数的脱敏规则
- 明确导出、对账、补偿、排障时查看敏感信息的权限和流程

## 敏感配置与依赖治理

- 说明密钥、令牌、凭证等敏感信息的处理方式。
- 禁止把密钥写入代码库、镜像、日志或默认配置。
- 依赖升级、漏洞扫描、镜像基线、第三方 SDK 接入要有检查机制。

## 接口签名、幂等与防重

- 哪些接口必须验签、验时间窗、验幂等键
- 哪些消费链路必须防重、去重、限流或分片串行
- 哪些关键操作必须具备人工重放与追溯能力

## 权限规则

记录人类开发者和代理都需要遵守的最小权限原则。

## 安全评审触发条件

列出哪些类型的改动必须触发额外安全评审。

## 应急与处置要求

- 发现越权、泄露、异常调用、供应链漏洞后的升级路径
- 临时封禁、回滚、密钥轮转、日志追溯的操作要求
