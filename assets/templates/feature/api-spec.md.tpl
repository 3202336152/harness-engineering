---
id: {{FEATURE_ID}}
title: {{FEATURE_TITLE}}
type: feature-api-spec
status: draft
owner: {{OWNER}}
change_types: "{{CHANGE_TYPES}}"
last_updated: {{DATE}}
template_version: {{TEMPLATE_VERSION}}
template_profile: {{TEMPLATE_PROFILE}}
template_language: {{TEMPLATE_LANGUAGE}}
---

# 接口规格

## 接口范围

描述新增或变更的 API 接口面。

## 接口清单

| 接口 | 协议 | 调用方 | 说明 | 是否新增/变更 |
|------|------|--------|------|---------------|
| 例如：查询用户 | HTTP GET | 前端/内部服务 | 查询用户详情 | 新增 |

## 接口详细设计

建议每个接口至少记录：

| 接口 | 入口类 | 编排服务 | 鉴权/验签 | 幂等方案 | 备注 |
|------|--------|----------|-----------|----------|------|
| 例如：创建订单 | `OrderController` | `OrderAppService` | JWT + 租户校验 | `requestId` + 唯一索引 | 主链入口 |

## 请求设计

- 路径、方法、RPC 方法名或消息主题
- 请求字段、必填校验、枚举取值、分页排序规则
- 幂等键、签名、鉴权上下文、租户/操作人透传方式

建议为关键接口补字段表：

| 字段 | 类型 | 必填 | 说明 | 校验规则 | 备注 |
|------|------|------|------|----------|------|
| 例如：orderId | String | 是 | 订单号 | 长度、格式、业务存在性 | 主业务键 |

## 响应与错误码

- 成功响应结构
- 业务错误码、系统错误码、可重试错误
- 空值、兼容字段、部分成功场景的表达方式

## 校验、幂等与安全

- 参数校验和越权校验放在哪一层
- 是否需要防重、防刷、频控、审计
- 哪些字段不能落日志或必须脱敏

## 典型示例与联调要点

- 补充典型请求/响应示例
- 说明联调环境、测试数据、签名规则、回调要求
- 说明是否影响旧客户端、旧调用方、旧消息消费者

## 兼容性与变更通知

- 字段新增、废弃、默认值变化如何处理
- 消费方、调用方、测试方需要提前同步哪些变更
- 是否需要灰度、双写、兼容旧版本报文

## 请求与响应格式

记录请求字段、响应字段和错误行为。
