# 核心信念

这份文档用于记录不应轻易改变的重要设计决策。

## 架构原则

- 记录长期稳定的分层边界和模块职责。
- 记录哪些规则必须靠代码或 CI 机械化执行，而不是只靠口头约定。

## 业务边界与领域模型

- 哪些业务能力必须由本系统负责，哪些必须交给外部系统。
- 哪些领域对象、状态机、一致性规则不能被轻易破坏。

## 技术选型

- 记录主要技术栈选择以及原因。
- 对 Java 项目，说明 JDK、框架、持久化、消息、缓存等关键选型及其适用边界。

## 一致性与数据原则

- 记录事务边界、幂等策略、补偿原则、缓存一致性原则。
- 记录哪些数据必须强一致，哪些允许最终一致。

## 接口与兼容性原则

- 记录对外接口、消息、数据库变更的兼容性底线。
- 记录字段废弃、版本升级、灰度发布时必须遵守的规则。

## 质量标准

- 记录不可妥协的工程标准。
- 例如测试门禁、代码评审红线、日志审计要求、性能基线、安全底线。
