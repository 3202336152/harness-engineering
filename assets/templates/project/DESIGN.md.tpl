---
id: project-design
title: 项目设计原则
type: project-design
status: active
owner: {{OWNER}}
last_updated: {{DATE}}
doc_state: scaffold
template_version: {{TEMPLATE_VERSION}}
template_profile: {{TEMPLATE_PROFILE}}
template_language: {{TEMPLATE_LANGUAGE}}
---

# 项目设计

## 设计目标

总结该项目在长期演进中要持续守住的目标。对复杂后端服务，设计目标不应停留在“分层清晰”，而应明确哪些规则必须收口、哪些链路必须可回放、哪些异常必须可恢复，例如：

- 业务规则集中而不是分散在 Controller/Job/Listener 中
- 技术实现可替换，核心领域规则尽量稳定
- 关键流程具备可观测、可回放、可补偿能力

## 工程设计结论

建议先写清楚当前项目的设计结论：

- 当前最核心的 2-3 条业务主链是什么
- 哪些模块是编排层，哪些模块是规则层，哪些模块是适配层
- 哪些地方已经收口良好，哪些地方仍存在职责混杂
- 哪些设计是当前已实现，哪些只是目标态

## 分层设计约定

建议说明以下内容：

- `controller / rpc / listener / job` 等入口是否统一归入接口层
- `application service / use case` 与 `domain service` 的职责划分
- `repository`、`mapper`、`dao`、`gateway`、`adapter` 的命名和定位
- `config`、`starter`、`support`、`common` 包或模块的进入门槛
- 单仓分包、多模块工程、微服务拆分时如何划定 parent/module/api/core/adapter 边界

建议补充：

- 对 Java/Spring 项目，补充启动类、配置类、自动装配类、定时任务类的放置规则
- 面向 HTTP、MQ、批处理三类入口时，各自的层次边界
- 单模块分包与多模块拆分的判定标准

## 对象模型与命名约定

建议至少覆盖：

- `Request / Response / DTO / Command / Query / VO / DO / Entity / Enum`
- 哪些对象允许跨层传递，哪些必须在边界处转换
- 集合、分页、金额、时间、状态字段的统一表达方式
- 是否统一使用 Lombok、MapStruct、Builder、Record 等

## 当前代码挂点与职责分工

建议用表格列出当前复杂链路的真实入口和落点：
如果仓库存在 `harness/.harness/runtime/java-doc-scan.json`，请先对照其中的 `application_services`、`domain_services`、`clients`，再补真实挂点。

| 业务能力 | 接口层入口 | 编排服务 | 规则/领域服务 | 数据/外部依赖 | 备注 |
|----------|------------|----------|---------------|---------------|------|
| 例如：下单 | `OrderController` | `OrderAppService` | `OrderDomainService` | `OrderMapper`、`PaymentClient` | 主链 |

这样写的目的是让开发者一眼看出“改某个需求应该先看哪里”。

## 持久化与数据设计约定

请说明：

- MyBatis XML、注解 Mapper、JPA Repository 的适用边界
- SQL 放置位置、命名规则、慢查询治理方式
- 乐观锁/悲观锁、唯一索引、状态机字段的使用约定
- 读模型和写模型是否分离，是否允许直接返回数据库对象

## 异常、错误码与可恢复性设计

- 业务异常、系统异常、第三方异常如何分层定义
- 是否统一错误码枚举和对外错误响应模型
- 哪些异常允许重试，哪些必须快速失败
- 补偿、回查、人工介入的触发条件

建议额外说明：

- `ControllerAdvice`、全局异常包装、日志落点如何统一
- 重复请求、并发覆盖写、部分成功、异步失败如何表达
- 哪些异常要保留原始上下游报文或关键业务键便于排障

## 集成与异步设计约定

- HTTP/RPC client 的封装规范
- 事件、消息、任务调度的命名和投递约定
- 出站事件是否采用 Outbox 或可靠消息方案
- 调用链中如何做超时、重试、熔断、隔离和降级

## 并发、缓存与任务编排

- 哪些流程允许并发执行，哪些必须串行或分片串行
- 缓存旁路、双删、主动失效、预热的统一做法
- 分布式锁、幂等表、去重键、任务重入控制的边界
- 批处理任务、补偿任务、重试任务和主链任务之间的关系

## 性能与扩展性关注点

记录团队在设计评审时必须主动检查的事项：

- N+1 查询、批量写入、索引缺失、长事务、全表扫描
- 热点缓存、热点 Key、消息积压、线程池耗尽
- 大对象序列化、深层对象拷贝、反射滥用
- 横向扩容、分库分表、多租户、灰度发布兼容性

## 实施顺序与改动原则

建议在真正开发前明确：

1. 先改入口校验还是先改核心规则。
2. 先补数据结构还是先补编排逻辑。
3. 是否需要先补测试桩、回归数据、观测指标。
4. 哪些改动必须拆阶段，哪些可以一次性完成。

## 设计评审检查清单

- 入口层是否只做协议处理，没有堆业务逻辑
- 事务是否只覆盖最小必要写操作
- 外部调用是否被适配层隔离
- 数据写入、删缓存、发消息顺序是否清晰
- 是否能说清楚失败重试、补偿、回滚路径
- 是否已经指定对应测试类、回归命令和证据产出

## 设计决策记录

将重要决策关联到 `harness/docs/project/核心信念.md` 或 `harness/docs/decisions/`，并说明：

- 决策背景
- 可选方案
- 最终选择
- 主要 trade-off
- 后续可演进方向
