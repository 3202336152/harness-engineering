---
id: {{FEATURE_ID}}
title: {{FEATURE_TITLE}}
type: feature-overview
status: draft
owner: {{OWNER}}
change_types: "{{CHANGE_TYPES}}"
last_updated: {{DATE}}
template_version: {{TEMPLATE_VERSION}}
template_profile: {{TEMPLATE_PROFILE}}
template_language: {{TEMPLATE_LANGUAGE}}
---

# 功能概览

## 文档定位

这份文档用于让开发、测试、评审快速理解：

- 这次需求为什么做
- 当前代码已经做到哪里
- 这次改动影响哪些入口、模块、表和上下游
- 验收时至少要看哪些结果

## 业务背景与目标

说明该功能为什么要做，它解决的业务问题是什么，目标用户是谁。

## 当前现状与边界

建议分开写清楚：

- 当前代码已实现的能力
- 当前代码未实现但需求希望补齐的能力
- 当前版本明确不处理的范围
- 现有已知技术债、限制条件或依赖外部前置

## 用户与入口

请补充：

- 谁会触发这个功能：用户、运营、系统、定时任务、消息消费者
- 功能入口：页面、HTTP API、RPC、MQ、批处理、管理台
- 上下游依赖：调用方、数据来源、下游写入方

## 上下游与依赖清单

建议至少列出：

| 类别 | 名称 | 作用 | 是否本次改动涉及 | 备注 |
|------|------|------|------------------|------|
| 上游接口 | 例如：PDC 回调 | 触发主链 | 是 | 需联调 |
| 表 | `t_order` | 核心数据 | 是 | 状态流转 |
| Topic | `order-created` | 异步触发 | 否/是 | 消费需幂等 |
| 缓存 | `order:detail:{id}` | 查询缓存 | 是 | 注意失效 |

## 范围

说明哪些内容在范围内，哪些内容明确不在范围内。

## 依赖与前置条件

- 依赖的配置、字典、开关、库表、接口、消息主题
- 需要哪些上游联调或前置数据准备

## 验收标准

- [ ] 在这里补充可验证、可度量的验收标准。

## 成功指标

说明上线后关注哪些业务指标、技术指标或人工效率指标。

## 关联文档与阅读顺序

建议补充开发推荐阅读顺序，例如：

1. 需求或背景文档
2. 本文
3. `方案设计.md`
4. `接口设计.md` / `数据设计.md`
5. `测试方案.md`
6. `发布回滚.md`
