---
id: project-api-spec
title: 项目接口规范
type: project-api-spec
status: active
owner: {{OWNER}}
last_updated: {{DATE}}
template_version: {{TEMPLATE_VERSION}}
template_profile: {{TEMPLATE_PROFILE}}
template_language: {{TEMPLATE_LANGUAGE}}
---

# 项目接口规范

## 文档定位

本文覆盖项目对外提供或消费的接口规范，适用于 REST、RPC、消息事件、回调接口等。
对复杂 Java 服务，接口规范除了字段定义，还必须明确入口类、编排服务、幂等方式、错误码、联调样例和兼容策略。

## 协议与接口类型

请按项目实际补充：

- HTTP / REST
- RPC（Dubbo、gRPC、内部协议）
- MQ Event / Command / Notification
- 定时任务或批处理的输入输出契约
- 第三方回调或 webhook

## 接口清单

建议使用表格维护当前正式口径：

| 接口名 | 协议 | 方向 | 调用方/消费方 | 主要用途 | 对应入口类 | 链接到详细文档 |
|--------|------|------|---------------|----------|------------|----------------|
| 例如：查询用户 | HTTP GET | 入站 | 前端/内部服务 | 用户详情查询 | `UserController` | `docs/features/.../接口设计.md` |

## 通用报文与上下文

- 认证身份、租户、操作人、traceId、requestId 的传递规则
- 时区、语言、币种、分页参数的统一约定
- 幂等键、签名、版本号、客户端标识的使用方式
- 如果存在 `BaseReq/BaseResp`、统一 Header、签名包装或回调包体，在这里固定口径

## 接口详细设计

建议每类接口至少写明：

- 路径/方法名/Topic/Job 名称
- 调用方与响应方
- 对应 `controller/listener/job`
- 对应 `application service`
- 是否鉴权、验签、幂等、防重、限流
- 超时、重试、降级、审计要求

## 请求设计

- 优先使用明确字段而不是 `Map<String, Object>`
- 使用 Bean Validation 或同类机制表达参数约束
- 对枚举、时间、金额、分页参数采用统一格式
- 批量接口要说明数量上限、顺序语义和部分失败策略

建议补充字段表：

| 字段 | 类型 | 必填 | 说明 | 校验规则 | 备注 |
|------|------|------|------|----------|------|
| 例如：orderId | String | 是 | 订单号 | 长度、正则、业务存在性 | 幂等键之一 |

## 响应体与错误码规范

- 统一成功响应结构、业务码、系统码
- 对可重试错误、业务拒绝、权限不足、资源不存在分别给出约定
- 明确空值、空数组、默认值、兼容字段的处理原则

建议建立错误码表：

| 错误码 | 含义 | HTTP/RPC 状态 | 是否可重试 | 处理建议 |
|--------|------|---------------|------------|----------|
| 例如：ORDER_NOT_FOUND | 订单不存在 | 404 | 否 | 检查业务参数 |

## 幂等性、分页与兼容性

- 新增、更新、消费类接口说明幂等方案
- 查询类接口说明排序字段、分页游标或页码规则
- 版本升级、字段新增、字段废弃、默认行为变更的兼容策略

## 异步事件与任务契约

- 消息 Topic / Tag / Consumer Group / Key 规范
- 消费顺序、并发度、重试、死信、补偿策略
- 定时任务的输入范围、扫描条件、幂等键和告警要求

## 安全、审计与可观测要求

- 哪些接口必须鉴权、鉴租户、验签或审计
- 哪些字段需要脱敏或禁止落日志
- 接口调用最少需要输出哪些日志、指标、trace 信息

## 典型示例与联调约定

- 补充典型请求/响应示例
- 说明联调环境、Mock 地址、回调地址、签名方式、测试数据准备
- 说明一次联调至少要验证哪些正常、异常、幂等和重试场景

## 共享 Schema 与示例来源

- 链接 OpenAPI / Protobuf / AsyncAPI / JSON Schema 的真实来源
- 链接 Mock、联调环境、示例请求与响应
