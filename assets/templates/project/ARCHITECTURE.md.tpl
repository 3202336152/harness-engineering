---
id: project-architecture
title: 项目架构
type: project-architecture
status: active
owner: {{OWNER}}
last_updated: {{DATE}}
template_version: {{TEMPLATE_VERSION}}
template_profile: {{TEMPLATE_PROFILE}}
template_language: {{TEMPLATE_LANGUAGE}}
---

# 项目架构

## 文档定位

本文是项目级共享架构基线，面向开发、评审、测试、运维和 AI 代理。
对于复杂后端项目，这份文档必须优先讲清楚当前系统的模块边界、主链路、关键表、消息链路、事务边界和排障入口，而不是只写抽象原则。Java/Spring 项目可直接按这里的分层与落点补充真实实现。

## 当前模板画像

- 模板版本：`{{TEMPLATE_VERSION}}`
- 模板画像：`{{TEMPLATE_PROFILE}}`
- 画像说明：{{PROFILE_DESCRIPTION}}

## 架构基线

建议先固定三类基线，避免后续文档口径漂移：

- 代码基线：当前仓库、分支、模块范围、最后一次对齐日期
- 需求基线：需求文档、评审结论、接口约束、对外承诺版本
- 运行基线：部署拓扑、中间件、数据库、缓存、消息、定时任务

## 系统上下文

建议至少写清楚以下内容：

- 仓库负责的核心业务能力和边界
- 核心参与方：用户、运营、内部系统、第三方系统
- 主要入口：HTTP API、RPC、MQ、定时任务、批处理、管理后台
- 关键外部依赖：数据库、缓存、消息中间件、搜索、对象存储、外部服务

## 当前架构结论

建议先给出 5-10 行结论，明确：

- 当前已经稳定落地的主链路
- 仍处于预留、改造中或待补齐的能力
- 当前最重的模块和主要风险点
- 哪些能力依赖外部系统、消息链路或批处理任务

## 模块清单与职责

建议使用表格维护当前正式口径：

| 模块/领域 | 主要职责 | 关键入口/出口 | 关键数据/中间件 | 当前状态 |
|-----------|----------|---------------|-----------------|----------|
| 例如：order | 下单、状态流转、事件触发 | HTTP + MQ | `t_order`、Redis、Kafka | 已实现 |

## 业务域与模块边界

建议继续补充领域边界，而不只是技术模块：

| 领域/模块 | 主要职责 | 对外暴露能力 | 不应承担的职责 |
|-----------|----------|--------------|----------------|
| 例如：user | 用户资料、身份状态 | 用户查询、用户更新 | 支付、订单编排 |

## 分层与包结构

推荐后端服务优先按照“接口层 -> 用例编排层 -> 核心规则层 -> 基础设施适配层”组织代码。以下示例以 Java/Spring 项目为例，单体、多模块、微服务都可参考；其他技术栈可按等价职责映射。

```text
interfaces (controller/rpc/listener/job)
  -> application
  -> domain
  -> infrastructure
```

补充约定：

- 启动类和根包建议位于业务代码最上层，例如 `com.company.project`。
- `interfaces` 层负责协议转换、参数校验、鉴权上下文接入。
- `application` 层负责编排用例、事务边界、权限校验、幂等控制。
- `domain` 层负责核心业务规则、聚合、一致性约束。
- `infrastructure` 层负责数据库、缓存、MQ、第三方 SDK、RPC client 的技术实现。
- 公共组件进入 shared/common 前，先证明其跨模块复用价值。

如果是多模块 Maven/Gradle 项目，建议额外列出：

- 根工程与子模块清单
- 每个子模块的包前缀和职责
- 哪些模块允许被依赖，哪些只能被动调用

## 分层模型

记录团队与代理都必须遵守的依赖流向。

```text
Types -> Config -> Repo -> Service -> Runtime -> UI
```

## 核心链路与时序

至少记录 2-4 条真正影响交付的主链：

1. 入站请求或消息如何进入系统。
2. 由哪些 `controller/listener/job -> application service -> domain service -> repository/client` 完成主流程。
3. 哪一步会写数据库、删缓存、发消息、调外部接口。
4. 哪些步骤依赖 `afterCommit`、Outbox、重试任务或补偿任务。

建议每条链路都补充：

- 入口类
- 核心服务类
- 关键表
- 关键缓存 Key / Topic / Job
- 失败后的处理方式

## 核心调用链路

建议按“调用顺序 + 代码挂点”描述，而不是只画图：

1. 外部请求如何进入系统，以及在哪一层做参数校验和鉴权。
2. 核心业务逻辑由哪些 application service / domain service 承担。
3. 数据写入、事件发布、缓存更新、外部调用的先后顺序。
4. 异常、超时、降级、补偿分别在哪一层处理。

## 核心数据与中间件

建议建立一份最小总表：

| 类别 | 名称 | 用途 | 写入方 | 读取方 | 风险/说明 |
|------|------|------|--------|--------|-----------|
| 表 | `t_order` | 订单主表 | order-service | query/job | 主状态流转 |
| 缓存 | `order:detail:{id}` | 查询缓存 | application | controller | 注意双写一致性 |
| Topic | `order-created` | 出站事件 | application | downstream | 消费需幂等 |
| Job | `OrderRetryJob` | 重试/补偿 | scheduler | application | 必须可重入 |

## 事务边界与一致性

请明确项目正式口径：

- 事务通常由哪一层开启，是否允许跨多个聚合或多个 repository。
- 本地事务、分布式事务、事件最终一致性各自的使用边界。
- 外部 RPC / HTTP 调用是否允许放在数据库事务中。
- 幂等、去重、重试、补偿、回查的统一策略。

复杂 Java 链路建议额外说明：

- 哪些流程通过 `@Transactional` 承担本地事务
- 哪些消息必须事务提交后才能发送
- 哪些流程需要分布式锁、唯一索引或幂等表
- 哪些失败允许自动重试，哪些必须人工介入

## 数据访问与持久化约定

结合项目实际说明：

- 使用 MyBatis / JPA / MyBatis-Plus / jOOQ 等哪种持久化方案。
- `Entity / Aggregate / DO / PO / DTO / VO` 的职责边界。
- 是否允许跨领域直接查表，是否必须通过领域服务或防腐层访问。
- 分页、批量、索引、锁、读写分离、归档、软删的统一约定。

## 缓存、消息与异步任务

需要写清楚正式工程约束：

- Redis/本地缓存的使用场景、Key 规范、失效策略、双写风险。
- MQ Topic / Tag / Consumer Group 的归属和命名规则。
- 定时任务、批处理、异步线程池的入口、限流、重试、告警要求。
- 消息消费与任务执行的幂等策略。

## 外部集成与防腐层

列出外部系统接入规范和当前真实落点：

- 统一通过 client / gateway / adapter 封装外部调用。
- 禁止业务代码散落第三方协议细节。
- 外部依赖的超时、重试、熔断、降级、审计要求。
- 如果存在老系统或异构协议，说明防腐层的放置位置和转换规则。

## 可观测性与排障入口

至少明确：

- 哪些日志能定位入口参数、业务键、外部调用、落库结果
- 哪些指标用于放量判断和故障止血
- 哪些表、缓存、死信、重试记录是线上排查第一入口
- 常用 dashboard、日志检索条件、trace 检索字段

## 可观测性与运行保障

至少明确：

- 日志字段规范：`traceId`、`requestId`、`operatorId`、`tenantId` 等。
- 指标与告警：成功率、耗时、错误码、消息堆积、任务失败数。
- 链路追踪、审计日志、业务埋点的落点。
- 线上问题排查时依赖哪些 dashboard、日志、SQL、事件记录。

## 安全与合规边界

记录接口鉴权、数据脱敏、权限校验、操作审计、敏感配置管理的统一原则，
并链接到 `docs/project/安全规范.md` 的详细要求。

## 架构演进与文档同步要求

- 重要链路改造时同步更新模块职责、主链路和关键表说明。
- 新增消息、任务、缓存、外部接口时同步更新本文和对应功能文档。
- 设计目标与当前实现状态必须分开写，避免把规划态内容写成已实现。

## 机械化约束

- 保持本文与 `.harness/architecture.json` 一致。
- 在 CI 中强制执行架构边界校验。
- 例外情况记录到 `docs/decisions/`。
- 重要模块重构时同步更新图示、调用链和边界约束。
